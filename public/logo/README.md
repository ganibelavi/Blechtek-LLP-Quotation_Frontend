Place your logo file here as `logo.png` (or `logo.jpg`).

The backend generator looks for the logo in two locations:

- `QuotationApp.API/Templates/logo.png` (preferred)
- `frontend/logo/logo.png` (alternate)

After placing the file, restart the backend and generate a sample quotation to verify the header image is shown.